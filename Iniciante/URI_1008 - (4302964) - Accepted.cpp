#include <bits/stdc++.h>

using namespace std;

int main(void)
{
	int a, b;
	double c;
	cin >> a >> b >> c;
	cout << "NUMBER = " << a << "\n" << "SALARY = U$ " << fixed << setprecision(2) << (b*c) << "\n";
	return(0);
}