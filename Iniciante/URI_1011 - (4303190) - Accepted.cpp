#include <bits/stdc++.h>

#define pi 3.14159

using namespace std;

int main(void)
{
	double r;
	cin >> r;
	cout << "VOLUME = " << fixed << setprecision(3) << (4/3.0)*pi*pow(r,3) << "\n";
	return(0);
}